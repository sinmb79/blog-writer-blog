# n8n Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add one-click n8n setup scripts plus five importable workflows for `blog-writer-blog`.

**Architecture:** Keep setup logic in root-level shell scripts, store workflow assets in a dedicated `n8n-workflows/` directory, and cover the new entrypoints with a small repository-structure regression test. The scripts should use repo-relative paths, avoid secrets, and degrade cleanly when optional services such as ComfyUI are absent.

**Tech Stack:** Bash, Windows batch, n8n workflow JSON, pytest

---

### Task 1: Add the failing repository-structure test

**Files:**
- Modify: `tests/test_structure.py`
- Test: `tests/test_structure.py`

- [ ] **Step 1: Write the failing test**

```python
def test_n8n_setup_assets_exist():
    ...
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_structure.py -v`
Expected: FAIL because `setup.sh`, `setup.bat`, or workflow files do not exist yet.

### Task 2: Add setup entrypoints and workflow assets

**Files:**
- Create: `setup.sh`
- Create: `setup.bat`
- Create: `n8n-workflows/blog-daily-pipeline.json`
- Create: `n8n-workflows/blog-write-queue.json`
- Create: `n8n-workflows/blog-publish-queue.json`
- Create: `n8n-workflows/blog-weekly-report.json`
- Create: `n8n-workflows/blog-monthly-reminder.json`

- [ ] **Step 1: Implement root setup scripts**
- [ ] **Step 2: Add five importable workflow JSON files**
- [ ] **Step 3: Keep workflow filenames aligned with the import commands**

### Task 3: Update installation docs

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace the top-level install section with the one-click n8n flow**
- [ ] **Step 2: Keep the beginner guide link for manual setup follow-up**

### Task 4: Verify and prepare GitHub publication

**Files:**
- Modify: `tests/test_structure.py`
- Create/Modify: all files above

- [ ] **Step 1: Run `python -m pytest tests/test_structure.py -v`**
- [ ] **Step 2: Run `python -m pytest -q`**
- [ ] **Step 3: Inspect `git status --short` and stage only the intended files**
- [ ] **Step 4: Commit with `feat: add setup.sh/bat for one-click n8n install + workflow import`**
- [ ] **Step 5: Push and create a GitHub release if repository permissions allow**
